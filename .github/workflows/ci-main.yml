name: Main branch integrity

on:
  push:
    branches: [main]
    paths:
      - '.agents/skills/**'
      - 'scripts/**'
      - 'evals/**'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  integrity:
    uses: ./.github/workflows/ci-pr.yml
